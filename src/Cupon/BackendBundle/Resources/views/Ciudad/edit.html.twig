{% extends '::backend.html.twig' %}

{% block id 'ciudad' %}
{% block title %}Modificar la ciudad{% endblock %}

{% block article %}
<h1>{{ block('ciudad') }}</h1>

<form action="{{ path('backend_ciudad_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Modificar</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('backend_ciudad') }}">
            Volver al listado
        </a>
    </li>
    <li>
        <form action="{{ path('backend_ciudad_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Borrar</button>
        </form>
    </li>
</ul>
{% endblock %}